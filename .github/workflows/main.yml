name: phpy
on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repository
        uses: actions/checkout@v4
      - name: ssh-connection
        uses: appleboy/ssh-action@v1.2.0

        with:
          host: ${{secrets.IP}}
          username: ${{secrets.USERNAME}}
          password: ${{secrets.PASSWORD}}

          script: |
           cd /home/sahil/gym
           git pull origin main
           pm2 delete "gym"
           pm2 start "php -S 0.0.0.0:8000" --name "gym"
           pm2 delete "admin"
           pm2 start "php -S 0.0.0.0:8001" --name "admin"
           pm2 restart all
           
      
